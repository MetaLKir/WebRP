набор мини-задач, каждая из которых решает отдельную практическую задачу по работе с DOM, событиями и обработкой пользовательского ввода.
Давай разберём всё по блокам:

🧩 Задача 1 — Изменение стиля текста (размер и цвет)

Есть текстовый элемент с классом 

Есть кнопки:

#size15Btn, #size25Btn, #size35Btn — меняют размер шрифта текста на 15px, 25px и 35px.

#redBtn, #greenBtn, #blueBtn — меняют цвет текста соответственно.

Идея задачи:
Показать, как можно через свойство style изменять CSS-параметры (например, fontSize и color) в ответ на действия пользователя.

🎨 Задача 2 — Изменение стиля текста через пользовательский ввод

Что делает код:

Есть два поля ввода:
 — пользователь вводит размер шрифта.

 — пользователь вводит цвет (например, red или #ff0000).

Кнопки:

 — вызывают одну и ту же функцию updateBottomText.

Эта функция меняет стиль .bottomText, задавая:

font-size из inpSize.value

color из inpColor.value


➕➖✖️➗ Задача 3 — Калькулятор

Цель:
Показать, как выполнять простые арифметические операции с числами, введёнными пользователем.

Что делает код:

Четыре блока: сложение, вычитание, умножение, деление.

Каждый блок имеет два поля ввода  и кнопку.

При нажатии:

addBtn → складывает числа и показывает результат.

subBtn → вычитает.

multiBtn → умножает.

divBtn → делит (с проверкой на деление на ноль).

красивые варианты с полноценным калькулятором приветствуются

🖼 Задача 4 — Галерея с переключением изображений

Цель:
Показать, как переключать элементы (например, картинки) на странице при клике на кнопки «вперёд» и «назад».

Что делает код:

Получает список элементов (например, картинок или блоков).

Есть кнопки #prevBtn и #nextBtn.

При нажатии:

текущий элемент скрывается (display = 'none');

индекс сдвигается (вперёд или назад);

новый элемент показывается (display = 'block').

При достижении конца/начала списка переключение идёт по кругу.



🔤 Задача 5 — Проверка слова на палиндром


Что делает код:

Пользователь вводит текст в поле 

При нажатии #checkBtn проверяется, является ли введённое слово палиндромом.

Результат выводится в элемент #result:

если палиндром → текст Palindrome! (зелёный цвет);

если нет → Not a Palindrome! (красный цвет).

Проверка реализована функцией isPalindrome, которая сравнивает символы с начала и конца строки.

а так же написать библиотеку на эвентах и доме, вместо консоли и промптов