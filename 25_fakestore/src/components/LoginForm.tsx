import type {FC} from "react";

interface LoginFormProps {
    // think what should be here!!!
}

export const LoginForm:FC<LoginFormProps> = () => {
    return (
        <div>

        </div>
    );
};
