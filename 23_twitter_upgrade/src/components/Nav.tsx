// import type {User} from "../types";
import {UserAvatar} from "./UserAvatar.tsx";

export const Nav = () => {
    return (
        <div className="nav">
            <UserAvatar size={"small"}/>
        </div>
    );
};
